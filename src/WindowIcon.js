import openWindow from "./assets/open_window.png"

function Window () {
    return (
        <span>
            <img src={openWindow} className="window_icon"/>
        </span>
    )
}

export default Window